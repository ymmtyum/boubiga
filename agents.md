# boubiga Agents Guide

## 目的
- このプロジェクトは、`docs/archive/iphone_mvp_design_doc.md` を元に始まった iPhone 特化の所有・使用感・買い替え判断アプリの実装を進めるための作業ルール集です。
- まずは SwiftUI の動く実用MVPを保ち、ローカル永続化まで入れたあと、通知や外部連携を段階的に統合します。

## このプロジェクトの運用ルール
- 返答する際は、ユーザーに次にしてほしいアクションを必ず伝えます。実機テストの依頼なのか、相談事項への返答待ちなのか、確認観点は何かまで明示します。
- 会話の中でコンセプト、機能方針、使用技術、運用方針の変更が確定したら、直ちにこの `agents.md` と必要に応じて関連ドキュメントを更新します。
- `agents.md` を見れば最新の進め方と運用ルールが追える状態を維持します。
- 実装時に参照する最新計画は `boubiga_implementation_plan.md` に一本化します。新しい方針が確定したら、このファイルを更新します。
- 実装が完了したら、同じ作業の中で `boubiga_implementation_plan.md` を更新します。完了したステップは未完了タスクとして残さず、必要に応じて `docs/archive/docs_archive.md` へ移します。
- 実装済みの機能概要が変わった場合は、同じ作業の中で `current_features.md` も更新します。
- 対応済みタスクや過去の方針変更は `docs/archive/docs_archive.md` に移します。
- `issues.md` で管理している項目は、対応が完了したら未処理の欄に残しっぱなしにせず、必要に応じて `docs/archive/docs_archive.md` へ移します。

## 現在の前提
- UI は SwiftUI ベースです。
- 画面の主実装は `boubiga/ContentView.swift` にあります。
- サンプルデータと表示用モデルは `boubiga/AppModels.swift` にあります。
- 実用MVPでは SwiftData やバックエンド永続化に広げる前に、`UserDefaults` + JSON 保存で、初回登録・カラー選択・バッテリーOCR結果・使用感ログ・相場確認状態が再起動後も残る状態を優先します。この最小ローカル永続化は実装済みです。
- サンプルデータは Preview、開発確認、デバッグ用途に限定し、実機利用では保存データまたは現在端末検出から始めます。
- 現在の MVP は iPhone 特化で、タブ構成を廃止し、`マイiPhone` ホーム1画面に集約しています。
- ローカル配信ルールの土台として、`bundled_app_config.json`、`DeviceSnapshot`、`RemoteAppConfig`、`RuleEngine` を追加済みです。ホームのインサイト表示と診断結果の一部へ接続済みで、やることリスト本体への全面接続は次フェーズで段階的に進めます。
- Pro買い切りのアプリ側土台として、StoreKit 2の `EntitlementManager`、Product ID `boubiga_pro_lifetime`、Paywall、購入・復元・ローカルPro判定を追加済みです。Apple Developer / App Store Connect側は Team ID `RSB2T5LYYP`、Bundle ID `com.boubiga`、Product ID `boubiga_pro_lifetime` で進めます。
- Supabaseサーバー土台として、`supabase/migrations` の初期DBスキーマ/seed、`get-published-config`、`preview-rules`、`publish-config` Edge Functions、`docs/server/supabase_design.md` を追加済みです。実接続にはSupabaseプロジェクト作成、migration適用、管理者owner追加、Edge Functionsデプロイ、環境変数設定が必要です。
- 初回導線では、iPhone 検出 → 写真つきの機種確認 → カラー選択 → 登録完了までをオンボーディングとしてまとめ、終わったらすぐホームに入る方針です。
- オンボーディングのカラー選択では、全カラーを写真つきカード一覧で表示し、写真・色名・選択状態で選べる形を優先します。対応画像がない場合は代表画像や色スウォッチにフォールバックします。
- 検出できた機種は原則確定扱いにし、モデル選択は「違うiPhoneを選ぶ」を押した場合だけ補助フローとして出します。
- iPhone 16 Pro / 16 Pro Max のカラー候補は、Apple公式仕様に合わせてブラックチタニウム、ホワイトチタニウム、ナチュラルチタニウム、デザートチタニウムの4色にします。
- `マイiPhone` は、あなたの iPhone を主役にしつつ、iPhoneカード、2カラムの状態メーター、使いづらさを解消する導線をまとめたホームとして扱います。
- ホーム再設計を含む最新方針は `boubiga_implementation_plan.md` を優先します。
- `マイiPhone` では、バッテリー残量や過去の iPhone 一覧は常設せず、買い替え判断に必要な情報だけを段階的に見せます。
- `マイiPhone` では、常設の `やることリスト` カードは置かず、右上のチェックアイコンに確認待ち件数を数字で表示します。タップ後はまず一覧で何をするべきかを見せ、項目を選ぶと各アクション画面へ進みます。
- `マイiPhone` では、登録日・最終更新日をホーム常設にせず、詳細画面やログ文脈で扱います。
- 状態メーターには右上に情報ボタンを置き、用語の意味を短く確認できるようにします。
- 状態メーター内の小さな補足文は、確認日・出典・使用量などの根拠情報に統一し、アクション誘導はやることリスト側へ寄せます。
- バッテリー情報は、設定アプリの「バッテリーの状態」画面スクショをOCRし、最大容量・充放電回数・製造日・最初の使用を取得する導線を優先します。手入力はフォールバックとして残します。
- 過去の iPhone の手動追加導線は常設の `+` ボタンではなく、いったん削って全体のノイズを下げています。
- 診断タブは置かず、ホーム最下部に `iPhone診断` カードだけを配置します。AirPods診断とSIM診断は現時点では表示しません。1画面構成のシンプルなデザインを優先します。
- 初回登録は極力軽くし、購入日・購入価格・満足度ではなく、現在端末の推定情報を起点に登録する方針です。
- バッテリー最大容量は公開APIで直接取得できないため、設定画面スクショのOCRを優先し、最大容量・充放電回数・製造日・最初の使用を確認日つきで保存します。
- バッテリーOCR導線は、フォーム入力よりも大きなスクショアップロード枠と読み取り結果カードを主役にします。手入力はフォールバックとして残します。
- 使用感はレビューではなく時系列の `使用感ログ` として扱い、MVPでは4問程度の軽いインタビュー形式を優先します。
- 下取り・売却相場は自動取得せず、MVPではゲオ参考買取価格のテスト値または外部確認導線で扱います。
- 最新OSの詳細、価格、容量削減などのガイドは、将来的に運営側で管理するサーバーから配信する方針です。MVPではローカル定義やテスト値で表示します。
- 診断はAIではなく、バッテリー、容量、動作、サイズ、カメラ、買い替え意向を使ったルールベースで始めます。
- ホームは文字説明をなるべく減らし、写真、色、グラフ、大きめのアイコン、短いマイクロコピーで状態が伝わるようにします。

## 変更方針
- 仕様の判断に迷ったら、まず `boubiga_implementation_plan.md` を優先します。`docs/archive/iphone_mvp_design_doc.md` は初期設計の参考資料として扱います。
- MVP では「iPhone の状態を少しずつ育てる体験」を最優先し、周辺機能は薄く始めます。
- 破壊的なリファクタより、小さく安全な変更を重ねます。
- デザイン追加時は、棚 UI よりも「あなたの iPhone が主役」「状態が自然にわかる軽さ」を優先します。
- デザイン追加時は、情報を足すより減らす方向を優先し、自動取得情報や過去端末一覧も不要なら出さない判断を取ります。
- 未入力や更新待ちを表現する場合は、「やること」「未完了」「確認待ち」の事務感を抑え、関連する状態カードの中で軽い通知・記録導線として扱います。
- 「使いづらさを解消する」には、注意項目がある場合だけ「解決する」導線を置き、具体策はシートや詳細画面で短く見せます。
- 表示ロジック変更時は `AppModels.swift` の現在端末、過去端末、タスク、ログの整合性を確認します。

## ファイル運用ルール
- 新しいドメインモデルやモックデータは、まず `boubiga/AppModels.swift` か責務の近い新規ファイルへ追加します。
- 画面が大きくなりすぎたら、`ContentView.swift` からタブ単位または再利用 View 単位で分割します。
- ローカル永続化や将来 Supabase 連携を始める場合は、View に直接保存・通信処理を書かず、`Client` / `Repository` / `Store` 的な層を分けます。
- 仕様メモや意思決定記録は、コードコメントに埋めすぎず Markdown ファイルで残します。
- 最新仕様や実装計画は `boubiga_implementation_plan.md` に集約し、分散した計画書を増やさないようにします。
- 現在の機能概要は `current_features.md` にまとめます。
- 実装完了後は、`boubiga_implementation_plan.md`、`current_features.md`、`docs/archive/docs_archive.md` の3つを確認し、最新計画・実装済み機能・過去ログの置き場所を整理します。

## 実装ルール
- 表示文言は原則日本語で統一します。
- 金額は `JPY` 表示を基本にします。
- ステータス、バッジ、カテゴリなどは文字列直書きより `enum` を優先します。
- 集計値は View 内で重複計算しすぎず、モデル側の計算プロパティへ寄せます。
- 外部 API 前提の機能は、まずモック UI とダミーデータで体験を固めてから本接続します。

## テストと確認
- 変更後は最低でもビルド確認を行います。
- 基本コマンド:
```bash
xcodebuild -project boubiga.xcodeproj -scheme boubiga -sdk iphonesimulator -configuration Debug build
```
- テストを回す場合は具体的な Simulator を指定します。
```bash
xcodebuild -project boubiga.xcodeproj -scheme boubiga -destination 'platform=iOS Simulator,name=iPhone 17' test
```
- UI 変更時は、ビルド成功だけでなく Preview または Simulator でタブ遷移とレイアウト崩れも確認します。

## 今後の拡張優先順
- 1. バッテリー最大容量入力フローの磨き込み
- 2. StoreKit商品の実機価格表示・購入・復元・Pro判定確認
- 3. `RuleEngine` の結果をやることリスト本体へ段階接続
- 4. 使用感ログの質問設計と記録体験
- 5. ホーム内の買い替え候補ロジック
- 6. Supabaseプロジェクトへのmigration適用とEdge Functionsデプロイ
- 7. iOS側の `AppConfigClient` / `AppConfigStore` をSupabase公開JSONへ接続
- 8. 現在端末の自動検出 UI の精度向上
- 9. 通知
- 10. 将来的な集合知や分析機能

## やらないこと
- いきなり全画面を本番 API 前提に作り替えない
- View に大量の状態管理と通信処理を同居させない
- 仕様書にない収益導線や購買フローを勝手に追加しない
- ユーザーの既存変更を無断で巻き戻さない
