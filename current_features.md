# boubiga 現在の機能

詳細な画面構成とデザイン方針は `docs/current_product_design.md` にまとめています。このファイルは機能概要の要約として扱います。

## アプリ概要

boubiga は、iPhone の所有状態と使用感を軽く記録し、買い替え判断につなげるための iPhone 特化アプリです。

## オンボーディング

- 起動時にあなたの iPhone 情報を検出します。
- `iphone_identifiers.json` の端末識別子マップを使って、ハードウェア識別子から機種名を取得します。
- 「ご使用のiPhoneはこちらで正しいですか？」で検出した機種名を大きく確認できます。
- 検出情報には端末写真、機種名、容量、空き容量、iOS を表示します。
- 「違うiPhoneを選ぶ」から、例外的に最近の iPhone モデルを選び直せます。
- 「どのカラーを使っていますか？」でモデルに応じたカラーを選択できます。
- カラー選択では、全カラーを写真つきカード一覧で表示し、写真・色名・チェックマークで選択できます。
- iPhone 16 Pro / 16 Pro Max は、ブラックチタニウム、ホワイトチタニウム、ナチュラルチタニウム、デザートチタニウムの4色を表示します。
- 登録完了画面の「完了」ボタンからホームに進みます。
- 初回起動時はサンプルデータではなく、保存データがなければ現在端末の検出情報からオンボーディングを開始します。

## マイiPhone

- あなたの iPhone を主役にしたホーム1画面構成です。
- 端末写真を中央に大きく表示し、その下に機種名、カラーを表示します。
- 画像アセットに白背景が含まれていても画面になじむよう、iPhone写真は共通表示部品で背景をなじませています。
- 常設のやることリストカードは置かず、ホーム右上のチェックアイコンに確認待ち件数を数字で表示します。タップ後はまず一覧で何をするべきかを見せ、項目を選ぶと各アクション画面へ進みます。
- やることリストには、保存済みの固定タスクに加えて `RuleEngine` の `todo` 出力も表示します。同じ導線に向かう項目は配信ルール側を優先して重複を抑えます。
- 登録日・最終更新日・現在の目安価値は主役の iPhone カードから外し、詳細画面または個別状態カードで扱います。
- 状態メーターとして、バッテリー、ストレージ、使用期間、参考買取価格、iOS情報を2カラムで表示します。
- 各状態メーター右上の情報ボタンから、バッテリー最大容量などの用語説明を確認できます。
- 状態メーターは大きめのアイコン、数値、色、グラフで状態を伝え、補足文は確認日・出典・使用量など根拠情報に統一しています。
- ストレージ空き容量とiOSバージョンは起動時に端末から取得し、あなたの iPhone の最新スナップショットとしてホームに反映します。
- 最新iOSは Supabase公開設定または同梱JSONの `latest_global_version` で判定します。iOS が最新ではない場合は、iOSカードを黄色にして「最新ではありません」と表示します。
- バッテリー最大容量が未確認の場合は、状態メーターの下に「さらに調べるためバッテリーについて教えてください」と大きめの誘導カードを表示し、スクショOCRへ進めます。
- バッテリー最大容量は公開APIで直接取得せず、設定アプリの「バッテリーの状態」画面スクショをOCRして、最大容量・充放電回数・製造日・最初の使用を取得できます。
- バッテリーOCRは、大きなアップロード枠からスクショを選んだあと、明示的に「スクショを読み取る」を押して読み取りを開始します。
- 読み取り結果は次の画面で、最大容量、充放電回数、製造日、最初の使用、バッテリー状態、使い始めてからの期間をカード表示で確認できます。
- OCR結果は通常は入力欄ではなく結果カードとして表示し、「修正する」を押した時だけ編集できます。
- 最大容量が読み取れていれば保存ボタンを押せます。最大容量が読めない場合は、保存ボタンから修正状態へ進めます。
- 現在のサンプル状態では、OCR導線確認のためバッテリー最大容量と使用期間を未入力にしています。
- 使用感はレビューではなく、時系列の使用感ログとして記録します。
- 使用感ログは将来の判断材料としてモデルに残しますが、ホームには常設しません。
- ホームでは、バッテリー残量、使用感カード、記録ログ、過去の iPhone 一覧は表示しません。

## 使いづらさの解消

- 診断タブは置かず、ホーム最下部にiPhone診断カードだけを表示しています。
- 「使いづらさを解消する」では、注意が必要な項目を1項目1カードで表示します。
- 注意項目には「解決する」ボタンを表示し、バッテリー、容量、使用感、相場ごとの具体策をシートで見られます。
- 「次の判断」カードはホームから廃止し、必要な判断材料は「使いづらさを解消する」と状態メーターに吸収しています。
- 記録ログは現状のホームでは使わないためカットしています。
- 下取り・売却相場は自動取得せず、まずはゲオ参考買取価格のテスト値として扱います。
- `RuleEngine` の結果を使い、ホーム上に確認・注意・解決につながるインサイトを最大3件表示します。

## ローカル永続化

- 登録した現在端末、カラー、バッテリー入力/OCR結果、使用感ログ、相場確認状態、診断ログ、確認待ちタスク状態を `UserDefaults` にJSONとして保存します。
- アプリ再起動時は保存済みデータを読み込み、現在端末のストレージ空き容量やiOSなどの検出スナップショットだけ最新化します。
- 保存済みタスクのうち、存在しない端末に紐づくものは読み込み時に除外します。
- 設定の「登録データを消してやり直す」から、現在端末登録、バッテリー記録、使用感ログ、相場確認、診断ログ、確認待ち状態を消して初回登録に戻せます。

## ローカル配信ルール土台

- `bundled_app_config.json` をアプリ内に同梱し、最新iOS、しきい値、表示ルールの初期値を読み込めます。
- `DeviceSnapshot` で現在端末の状態を統一形式に変換できます。
- `RuleEngine` で、端末状態とローカルルールを照合し、やること、注意、解決、診断ヒントに分類できます。
- ホームのインサイト表示、診断結果の一部、やることリスト本体は `RuleEngine` の結果を使っています。

## Supabaseサーバー土台

- Supabase用の初期DBスキーマを `supabase/migrations` に追加しています。
- 管理者プロフィール、設定バージョン、iOS情報、しきい値、ガイド、アクション項目、ルール、公開設定、監査ログを管理するテーブルがあります。
- RLSを有効にし、管理系テーブルは `admin_profiles` の `owner` / `editor` / `viewer` で制御する方針です。
- Data APIの自動公開はOFFにし、必要なテーブル権限だけをmigrationで明示的に付与しています。
- `published_configs` の current JSONだけをアプリ配信用として扱います。
- Edge Functionsとして、公開設定取得の `get-published-config`、管理者向けルール確認の `preview-rules`、公開処理の `publish-config` を用意しています。
- サーバー設計とデプロイ手順は `docs/server/supabase_design.md` にまとめています。
- SupabaseのIDやキーは `.env.local` に記入し、共有用の雛形は `.env.example` に置いています。記入手順は `docs/server/supabase_setup.md` にまとめています。
- Supabaseプロジェクトへのmigration適用、管理者owner登録、Edge Functionsデプロイ、環境変数設定は完了済みです。

## Supabase公開設定取得

- アプリ起動時に `get-published-config` Edge Function から公開設定JSONを取得します。
- 取得に成功すると `RemoteAppConfig` として読み込み、`UserDefaults` にキャッシュします。
- 通信に失敗した場合は、前回キャッシュまたは `bundled_app_config.json` の同梱値で動きます。
- iOSアプリには service role key を入れず、`supabase_public_config.json` に公開設定取得用の Supabase URL だけを同梱しています。

## Web管理画面

- `admin/` に Next.js ベースの管理画面MVPがあります。
- `admin/.env.local` には、既存の `.env.local` から Supabase URL と anon key だけを入れてローカル起動できるようにしています。
- 管理画面は Supabase Auth でログインします。管理操作はログインユーザーのアクセストークンで `preview-rules` / `publish-config` を呼びます。
- 管理者ユーザーにパスワードを設定していない場合は、メールアドレスを入力してログインリンクを送信できます。
- 管理画面へのログインは確認済みです。
- 公開中の設定バージョン、公開日時、最新iOS、ルール数、しきい値を確認できます。
- 最新iOS、リリース日、重要度、表示メッセージ、出典URLを下書きとして編集できます。
- バッテリー最大容量と空き容量のしきい値を編集できます。
- 端末スナップショットを入力して、下書きまたは公開済みルールのプレビューを確認できます。
- 公開ボタンから、現在のDB内容を新しい配信JSONとして公開する処理を実行できます。
- action_items / rules / guides の詳細編集UIは次フェーズです。

## 診断とPro導線

- ホーム最下部の iPhone診断カードから、簡易診断結果シートを開けます。
- 無料状態では、今の状態の要約、理由、無料でできるアクションを表示します。
- 詳細診断はPro向けとしてロック表示し、「詳しい診断を見る」からPaywallへ進みます。
- `EntitlementManager` でStoreKit 2の商品読み込み、購入、復元、ローカルPro判定を扱います。
- Pro買い切り商品のProduct IDは `boubiga_pro_lifetime` です。
- App Store Connect側では、Team ID `RSB2T5LYYP`、Bundle ID `com.boubiga`、買い切りPro商品 `boubiga_pro_lifetime` を使います。
- 商品が読み込めない状態では、Paywallに商品設定確認のメッセージ、Product ID、商品情報の再読み込みボタンを表示します。

## 設定

- ホーム左上の歯車から設定を開けます。
- 通知設定、データ管理、プライバシー、Pro機能などの項目をモック表示しています。
- データ管理から「登録データを消してやり直す」を実行できます。あなたの iPhone 登録と関連ログをリセットし、初回登録フローからやり直せます。

## 詳細

- あなたの iPhone カードを押すと詳細画面を開けます。
- 詳細画面の端末画像も、ホームと同じく選択中カラーに対応した画像を表示します。
- バッテリー欄では、OCRで保存した最大容量を優先し、未入力時は未入力として表示します。

## 現在の実装方針

- SwiftUI ベースで実装しています。
- 最小ローカル永続化、ローカルJSONルール、診断結果シート、Pro/StoreKit土台、App Store Connectの商品作成、Supabaseサーバー土台、iOS側の公開JSON取得、やることリスト本体のRuleEngine接続、管理画面MVPは完了済みです。次は管理画面の action/rule/guide 編集UI、または実機での購入・公開設定取得確認に進みます。
- 最新OSの詳細、価格、容量削減などのガイドは、将来的に運営側で管理するサーバーから配信する方針です。
- 画面はなるべくシンプルにし、あなたの iPhone とその状態がすぐ分かる構成を優先しています。
- 最新の実装計画は `boubiga_implementation_plan.md` に一本化しています。
